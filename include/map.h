#ifndef MAP_H
#define MAP_H

// map size must be integers/whole numbers
#define MAP_WIDTH 33
#define MAP_HEIGHT 17

// locks the map size, so other functions don't mess it up 
extern int map[MAP_HEIGHT][MAP_WIDTH];

#endif